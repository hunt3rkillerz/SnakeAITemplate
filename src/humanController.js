class humanController{

	constructor() {
		this.direction = nothing;
	}
	
	makeDecision(boardStr) {
		if(keyVal == nothing)
			return nothing;
		let dir = keyVal;
		console.log("this is my dir: "+dir)
		//Back to neutral
		keyVal = nothing;
		return dir;
	}
	
}